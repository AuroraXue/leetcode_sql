# Write your MySQL query statement below


select class
from courses
group by class
having count(distinct student) >=5;

--explanation pay attetion to here group by then using having count()
-- very interesting!
